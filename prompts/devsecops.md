HARD_MODE_DEVSECOPS_PROMPT:
role: >
Act as Senior DevSecOps + Senior QA + Staff Fullstack. Your job is to add
production-grade DevSecOps to the existing SOP Builder monorepo (Cloudflare-only)
while guaranteeing $0 USD infra until 100th retained customer.

objectives: - Enforce 3-stage pipeline: Local → Preview → Production (NO staging). - Strong CI gates: lint, typecheck, unit+integration+e2e, coverage >= 95%. - Security gates: CodeQL + "security regression pack" (pnpm test:security). - Preview-per-PR deployments: - Pages: unique preview URL per PR (native Cloudflare behavior). - Workers: preview URLs for non-prod verification; provide a stable alias for PR E2E when feasible. - Isolated preview data: - Prefer: separate D1 databases for preview vs prod. - Fallback (if quota prevents per-PR DB): shared preview DB but strict logical isolation by PREVIEW_NAMESPACE (PR number),
enforced server-side in every query path and tested. - Make deployments reproducible, documented, and rollbackable.

pinned_versions_latest_stable: # Use these minimums / pins in package.json to stabilize builds (update if newer stable exists).
pnpm: "10.30.1"
node: "20.x LTS"
wrangler: "4.66.0"
hono: "4.12.1"
drizzle_orm: "0.45.1"
vitest: "4.0.18"
playwright: "1.58.2"
eslint: "10.0.1"
typescript: "5.9"

repository_expectations: - Monorepo with pnpm workspaces. - Apps: worker-api, worker-consumer, tma-web. - Packages: shared (zod schemas), sop-engine. - Cloudflare: Pages, Workers, D1, R2, Queues. - Payments: Stars + WalletPay + TON (idempotent webhooks).

environment_contract:
LOCAL:
description: "Developer machine. Fast feedback."
runtime: - Miniflare for Workers - local sqlite (or D1 local simulation)
secrets: "Use .dev.vars locally; never commit."
data_isolation: "Local DB per developer."
gates: - pnpm lint - pnpm typecheck - pnpm test (>=95%) - pnpm test:integration
PREVIEW:
description: "Per-PR preview deployment + E2E verification."
url_requirements:
pages: "Unique PR preview URL auto-generated by Cloudflare Pages."
workers: "Use Workers preview URL or deploy to preview env. Capture URL for E2E."
secrets: - "No prod secrets." - "Use limited-scope tokens/keys, or feature-flag external payment confirmations."
data_isolation:
primary: "Dedicated D1_PREVIEW separate from D1_PROD."
fallback: "D1_PREVIEW shared; isolate by PREVIEW_NAMESPACE=pr-<number> and enforce in server queries."
gates: - All LOCAL gates - pnpm test:e2e (against preview URLs) - pnpm test:security - CodeQL
PRODUCTION:
description: "Main branch deploy with locked-down secrets."
secrets: "Cloudflare Worker secrets + GitHub Actions secrets."
data_isolation: "D1_PROD only."
gates: - All PREVIEW gates
release_controls: - "Branch protection: only merge to main via PR + required checks." - "Manual approval optional via GitHub Environments for prod."

required_scripts_to_add:
package_json_root: - "lint" - "typecheck" - "test" - "test:integration" - "test:e2e" - "test:security" - "build"
expected_commands:
lint: "pnpm -r lint"
typecheck: "pnpm -r typecheck"
test: "pnpm -r test -- --coverage"
test_integration: "pnpm -r test:integration"
test_e2e: "pnpm --filter tma-web test:e2e"
test_security: "pnpm -r test:security"
build: "pnpm -r build"

ci_cd_workflows_to_create: - path: ".github/workflows/ci.yml"
purpose: "PR gates: lint/typecheck/tests/coverage/integration/security" - path: ".github/workflows/e2e_preview.yml"
purpose: "Deploy preview (Pages + Workers) then run Playwright E2E against preview URLs" - path: ".github/workflows/codeql.yml"
purpose: "CodeQL scanning for JS/TS" - path: ".github/workflows/deploy_prod.yml"
purpose: "Deploy production on main, run smoke checks, output prod URLs"

workflow_design_details:
ci_yml:
triggers: ["pull_request", "push (main)"]
jobs: - name: "setup"
steps: - checkout - setup-node (node 20) - enable-corepack - pnpm install --frozen-lockfile - name: "lint_typecheck_unit"
steps: - pnpm lint - pnpm typecheck - pnpm test (enforce >=95% in vitest config) - name: "integration"
steps: - pnpm test:integration (miniflare) - name: "security_regression"
steps: - pnpm test:security
artifacts: - "coverage reports uploaded as artifacts"
e2e_preview_yml:
triggers: ["pull_request"]
jobs: - name: "deploy_pages_preview"
use_action: "Cloudflare Pages GitHub Action"
outputs_needed: - "pages_url" - name: "deploy_workers_preview"
use_action: "cloudflare/wrangler-action"
strategy:
preferred: "Use Workers preview URLs or a preview env; capture url for E2E."
notes: - "Workers have versioned/aliased Preview URLs; use them for PR verification." - name: "run_playwright_e2e"
needs: ["deploy_pages_preview", "deploy_workers_preview"]
steps: - set env:
TMA_BASE_URL: "${{ needs.deploy_pages_preview.outputs.pages_url }}"
                API_BASE_URL: "${{ needs.deploy_workers_preview.outputs.worker_url }}"
PREVIEW_NAMESPACE: "pr-${{ github.event.pull_request.number }}" - pnpm test:e2e
strictness: - "E2E must fail PR if login / SOP create / approval / publish flow fails."
deploy_prod_yml:
triggers: ["push (main)"]
jobs: - name: "deploy_pages_prod" - name: "deploy_workers_prod" - name: "migrate_d1_prod" - name: "post_deploy_smoke"
steps: - run minimal HTTP checks + basic bot webhook sanity

cloudflare_config_to_add:
wrangler_toml_requirements: - "Separate environments: [env.preview], [env.production]" - "Bindings per env: D1 database, R2 bucket, Queue" - "vars per env: ENV_NAME, PREVIEW_NAMESPACE (preview only), FEATURE_FLAGS" - "Keep secrets out of wrangler.toml; use wrangler secret"
pages_config: - "Use Cloudflare Pages previews for PRs (native). Provide the URL to CI as an output."

secrets_matrix:
github_actions_secrets: # least privilege API token recommended - CLOUDFLARE_API_TOKEN - CLOUDFLARE_ACCOUNT_ID - CLOUDFLARE_PAGES_PROJECT_NAME - CLOUDFLARE_WORKER_NAME_API - CLOUDFLARE_WORKER_NAME_CONSUMER - TELEGRAM_BOT_TOKEN_PREVIEW - TELEGRAM_BOT_TOKEN_PROD - ENCRYPTION_MASTER_KEY_PREVIEW - ENCRYPTION_MASTER_KEY_PROD - PAYMENT_KEYS_PREVIEW (optional/feature-flag) - PAYMENT_KEYS_PROD
worker_secrets_commands: - "wrangler secret put TELEGRAM_BOT_TOKEN --env preview" - "wrangler secret put TELEGRAM_BOT_TOKEN --env production" - "wrangler secret put ENCRYPTION_MASTER_KEY --env preview" - "wrangler secret put ENCRYPTION_MASTER_KEY --env production"

security_regression_pack_requirements:
must_test: - name: "telegram_initdata_tamper"
asserts: - "bad hash rejected" - "stale auth_date rejected" - name: "rbac_escalation"
asserts: - "Viewer cannot edit" - "Editor cannot approve" - "Approver cannot access other workspace" - name: "webhook_replay_idempotency"
asserts: - "same external_id processed once" - name: "job_enqueue_idempotency"
asserts: - "same interview session cannot create duplicate SOP versions" - name: "rate_limits"
asserts: - "burst generate triggers 429" - name: "prompt_injection_resistance"
asserts: - "malicious user text does not override system role or leak secrets"
command: "pnpm test:security"

docs_to_create_or_update: - "docs/deployment-guide.md" - "docs/runbook.md" - "docs/security.md" - "docs/incident-response.md"
must_include: - "Local dev setup (Miniflare), env vars, running tests" - "Preview deployment flow and how to retrieve URLs" - "Prod deployment flow + rollback" - "Bot token rotation procedure" - "D1 backup plan (export to R2) and restore procedure" - "Payment reconciliation fallback"

acceptance_criteria: - "PR cannot merge unless CI passes: lint/typecheck/unit>=95%/integration/security" - "Preview deploy runs on PR and E2E executes using preview URLs" - "Prod deploy runs on main with migrations and smoke checks" - "All secrets handled via GitHub Secrets + Wrangler secrets (never committed)" - "Preview data isolation enforced (DB separation or PREVIEW_NAMESPACE) + tested" - "CodeQL workflow active and passing"

execution_instructions_for_agent: - "Scan repo: list missing scripts, missing workflows, missing docs." - "Implement workflows and scripts first, then fix tests to pass." - "Add the security regression tests next (they are a merge gate)." - "Document everything; include exact commands and expected outputs." - "Finish by printing a checklist showing every acceptance criterion is met."
